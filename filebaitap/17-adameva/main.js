let adam = new Human('adam', 'Male', 80)
let eva = new Human('eva', 'Female', 45)
let apple1 = new Apple(10)
function run() {
    while (main.isEmpty()) {
        adam.eat(main);
        eva.eat(main);
    }
}